import React from 'react';
import { ArrowDownLeftIcon } from '@heroicons/react/24/outline';
import BaseButton from './BaseButton';

const LoginButton = (props) => {
  return <BaseButton text="Unlock" {...props} />;
};

export default LoginButton;
